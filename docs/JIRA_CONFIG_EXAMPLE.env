# JIRA Integration Configuration Example
# Copy these variables to your .env file and update with your values

# JIRA OAuth Configuration (Required)
# Your JIRA instance URL (without trailing slash)
JIRA_BASE_URL=https://your-company.atlassian.net

# OAuth2 Application Credentials from JIRA
# Create OAuth app in JIRA: Administration > Applications > OAuth credentials
JIRA_OAUTH_CLIENT_ID=your-oauth-client-id-here
JIRA_OAUTH_CLIENT_SECRET=your-oauth-client-secret-here

# OAuth Redirect URI (must match your iHub Apps instance)
# This should point to your iHub Apps instance + /api/integrations/jira/callback
JIRA_OAUTH_REDIRECT_URI=https://your-ihub-domain.com/api/integrations/jira/callback

# Security Configuration (Required for production)
# Generate a secure 256-bit key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=your-256-bit-encryption-key-in-hex-format

# Optional: Rate Limiting Configuration
JIRA_RATE_LIMIT_WINDOW=900000  # 15 minutes in milliseconds
JIRA_RATE_LIMIT_MAX=100        # Maximum requests per window per user

# Example OAuth2 Scopes (automatically configured)
# - read:jira-user    : Access user profile information
# - read:jira-work    : Read issues, projects, etc.
# - write:jira-work   : Create/update issues, add comments

# Setup Instructions:
# 1. Log into your JIRA instance as an administrator
# 2. Go to Administration > Applications > OAuth credentials
# 3. Create a new OAuth2 application with these settings:
#    - Application name: iHub Apps JIRA Integration
#    - Redirect URI: [your-ihub-domain]/api/integrations/jira/callback
#    - Scopes: read:jira-user, read:jira-work, write:jira-work
# 4. Copy the Client ID and Client Secret to the variables above
# 5. Generate a secure encryption key for TOKEN_ENCRYPTION_KEY
# 6. Restart your iHub Apps server
# 7. Users can now connect their JIRA accounts in the chat interface