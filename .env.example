# OpenAI API Key
GPT_3_5_TURBO_API_KEY=sk-...
GPT_4_API_KEY=sk-...

# Anthropic API Key
CLAUDE_3_OPUS_API_KEY=sk-ant-...
CLAUDE_3_SONNET_API_KEY=sk-ant-...

# Google API Key - use unified key for both models
GEMINI_API_KEY=AIza...
# (No need for separate keys for different Gemini models)
OPENAI_API_KEY=sk-...

# Microsoft speech Recognition Service
AZURE_SUBSCRIPTION_ID=...

# Server configuration
PORT=3000

# Proxy Configuration (optional)
# Configure HTTP/HTTPS proxy for external service calls
# These can also be configured in the platform.json file under the "proxy" section
#HTTP_PROXY=http://proxy.example.com:8080
#HTTPS_PROXY=http://proxy.example.com:8080
#NO_PROXY=localhost,127.0.0.1,.local

# Authentication Configuration
# JWT Secret for token signing (optional - auto-generated and persisted if not set)
# For multi-node deployments, set this explicitly so all nodes share the same secret.
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_VALUE

# Token Encryption Key (used for encrypting sensitive values in .env)
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If not set, a random key will be generated (not recommended for production)
#TOKEN_ENCRYPTION_KEY=your_64_character_hex_key_here

# LDAP/AD Authentication (optional)
# You can encrypt passwords using: node server/utils/encryptEnvValue.js "your_password"
# Encrypted values start with ENC[ and are automatically decrypted when loaded
#LDAP_ADMIN_PASSWORD=your_ldap_admin_password_or_ENC[...]
#AD_BIND_USER=your_ad_service_account
#AD_BIND_PASSWORD=your_ad_service_password_or_ENC[...]
