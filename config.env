# AI Hub Apps Configuration
# Copy this file to the same directory as your binary executable
# and customize the settings as needed

# Server Configuration
PORT=3000                # The port your server will listen on
HOST=0.0.0.0             # Set to 0.0.0.0 to accept connections from any IP, or 127.0.0.1 for local only
REQUEST_TIMEOUT=60000    # Timeout for LLM requests in milliseconds

# SSL Configuration (optional)
# Uncomment and set these values to enable HTTPS
#SSL_KEY=/path/to/your/key.pem     # Path to SSL private key
#SSL_CERT=/path/to/your/cert.pem   # Path to SSL certificate
#SSL_CA=/path/to/your/ca.pem       # Path to SSL CA certificate (optional)

# LLM API Keys (required for functionality)
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
GOOGLE_API_KEY=your_google_api_key